# 更新日志

本文件自 0.7.1 起记录。更早的版本没有条目。

## 0.7.1

**主题：把"抄一份值"改成"接宿主变量"。** 0.7.0 及以前，面板的字体、字号、底色都是从宿主
样式里复制出来的固定值 —— 复制值今天看着对，但用户一改设置、或宿主一换主题就静默失效，
而界面仍然"看起来正常"，没人会发现。

### 修复

- **会话面板不再无视「外观」里的正文字号。** 面板原来写死 `font:15px/1.47`，全文件不引用
  `--dsh-content-font-size`。会话区里 chat / conversation / tool / goal / workflow-run 等
  视图全部走宿主的字号变量，只有本面板不走。现在字号接 `--dsh-content-font-size`、行高接
  `calc(24px + var(--dsh-content-font-delta,0px))`，与相邻页签同一个口径。
  （影响：把字号设成 12px 的用户，面板从 15px 变为 12px/22px。）
- **面板底色不再与相邻页签有色差。** 原来写死 `#000`(暗) / `#f2f2f7`(亮)；会话根节点
  (`ConversationRoot`) 实际画的是 `--dsw-alias-bg-base`(`#151517` / `#fff`)。
  现在底色接该 token，`--d-bg` 退居兜底，切页签不再看到一整块异色。
- **字体族改接宿主钩子。** 面板与仪表盘页原来各自抄了一份字体栈（且两份不同）。现在都写
  `var(--dsw-font-family, <DSH 的同一串 fallback>)` —— `--dsw-font-family` 是 DSH 留给主题
  覆写的钩子，宿主换字体时两处一起跟随。
- **仪表盘页的「自动」主题改为跟随 DSH 外观，而不是只跟随操作系统。** 该页是独立文档，
  看不到宿主 DOM 上的 `data-ds-dark-theme`，因此由宿主在发送页面时注入
  `window.__DSH_TOKEN_THEME__`（读同一个 `ui-theme` 设置段）。此前当用户把 DSH 钉成深色而
  系统是浅色（或反之）时，仪表盘会和外壳反着来。
- **外观偏好纳入页面缓存键。** 页面缓存原来只按 `(path, mtime)` 失效；只改主题设置而不动
  页面文件时，注入的旧偏好会被一直发出去。现在缓存键含偏好，改设置立即生效。
- 主题切换的提示文案由含糊的「跟随系统」改为说明实际来源（如「跟随 DSH(深色)」）。
- 系统深浅变化的重绘收窄：DSH 把外观钉成浅色/深色时，系统深浅与本页无关，不再触发重绘。

### 测试

- 新增两条**宿主适配不回退**断言：面板必须接宿主字体族/正文字号/底色，且不得再用 `font`
  简写写死字号。这两条会在有人把绑定改回固定值时直接失败。
- 引用闭合检查的豁免从 `--dsw-*` 扩展到宿主的整个命名空间（含 `--dsh-*`，即内容字号这类
  外壳运行参数），并在注释里写清两个前缀各自代表什么。
- e2e 新增两条：页面必须带注入的外观偏好引导脚本（且位于 `<head>` 内、先于页面自身脚本）；
  只改设置、不动页面文件时注入值必须立刻换新。

### 说明

- 无破坏性改动：三个入口（侧边栏 / 对话区页签 / 设置页）的名称、插槽与 API 契约不变，
  `store.json` 结构版本不变。
- 侧边栏入口与设置页面板本来就正确使用 `--dsw-alias-*`，本次未改动。
