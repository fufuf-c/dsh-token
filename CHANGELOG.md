# 更新日志

本文件自 0.7.1 起记录。更早的版本没有条目。

> 版本摘要。**每条防退化守卫的"为什么存在"在 `scripts/invariants.mjs`**
> (112 条,编号 V1–V73e),由 `test/invariants.test.mjs` 强制与测试套件一一对应 ——
> 想判断"某个判断能不能删",查那张表,而不是翻本文。

## 0.9.6

**主题:修 dsh 0.1.7-alpha.1 上"插件时好时坏、打开面板要等很久才出数"的根因 ——
宿主把遗留会话的 revision 换成了"文件身份 + 整库语料哈希",而本插件用 revision 当
水位线,于是任一会话变动都让全部遗留会话失效、每轮整库重读。** 无存储格式变更
(`STORE_VERSION` 仍为 v8),**升级不需要迁移动作**;已在 0.1.7 上跑过的库第一轮就是零重读。

### 修复

1. **水位线只认"本会话文件身份",不再受整库语料哈希牵连**(`lib/session-source.mjs`)。
   宿主 `@deepseek-ai/dsh-session-persistence-jsonl` 0.1.7-alpha.1 起,`list()` 对
   `sourceVersion < 当前格式` 的会话返回
   `<dev:ino:size:mtimeNs:ctimeNs>:<historicalCorpusRevision()>`;后者是**整库所有
   会话文件**路径 + 各自 stat 身份的 sha256。旧实现在 `scanStore` 里直接做
   `wm.rev !== String(s.revision)`,粒度错配:

   | 场景(70 会话 / 65 个 v3,仅改 1 个文件 mtime,内容零改动) | 0.9.5 | 0.9.6 |
   |---|---|---|
   | 无变化的一轮 | 88 ms | 88 ms |
   | 只改 1 个文件的 mtime | **8 512 ms**（changed=65） | **~90 ms**（changed=0） |

   而页面「进入界面时先扫描」(默认开启)意味着打开面板要等整轮扫完才出数据 ——
   这正是"有时候不显示、过一会又能显示"。现在 `ownRevisionToken()` 只取前五段
   (文件身份);对新旧宿主都安全(≤0.1.6 本来就是五段,内存会话 `memory:<n>:<i>`
   段数不足则原样返回)。

2. **遗留会话的兄弟依赖改为精确追踪,而不是丢弃**(同文件)。
   整库哈希原本承载的语义是"遗留会话解码时可能读兄弟文件"(宿主
   `prepareStoredMigration` → `children()` 会读 `origin === 'subagent' &&
   parentSession === id` 的子会话参与解码)。直接删掉这层依赖会让父会话在子会话变化后
   不再重折(数据静默少算)。新增 `relatedFingerprints()`:按宿主**真正读的那批文件**
   算"父会话 id → 子会话 id+文件身份"指纹,并存入水位线的 `rel` 字段。于是子会话
   增删改只让**该父会话**重读,不波及别的父会话 —— 语义不弱于整库哈希,影响面从
   O(全库) 收到 O(该父的子树)。

3. **升级路径不会触发一次整库重读**(同文件)。已在 0.1.7 上跑过的库里,水位线存的是
   六段形态(含整库哈希)。比较时对**已存值与当前值都**做归一化,所以升级后第一轮
   就是零重读,而不是把全部遗留会话判为"变了"。

4. **`dsh.engines.dsh` 补上 `>=0.1.7-0`**(`package.json` / `README.md`)。
   按 SemVer 规则预发布不落在普通范围里:`0.1.7-alpha.1` 既不满足 `>=0.1.5-rc.1`
   也不满足 `>=0.1.6-0`,旧声明把本插件**实际支持并已在跑**的 0.1.7 预发布判成不兼容
   (插件市场会据此显示"不兼容")。同时把 `0.1.7-alpha.1` 记进 `compatibility.dshReleases`。

### 测试

5. **`test/v096-hostrev.test.mjs`(V73–V73e)**。用宿主 0.1.7 的真实 revision 形态
   (含"20 个会话共享同一语料哈希")锁住:任一会话变动只重读它自己;语料哈希变化本身
   零重读;老库六段水位线零重读;子会话变化只波及它的父;以及两个纯函数的边界。
   这四条行为断言在旧逻辑下**实测有 4 条失败**,不是事后补的空测试。

## 0.9.5

**主题:补掉唯一一处"与改动量脱钩"的计算 —— 扫描收尾的聚合重建从"只要有会话变化就
整库重折"改成"只重折被改动的会话"。** 无存储格式变更(`STORE_VERSION` 仍为 v8),
**升级不需要迁移动作**;重启后第一轮扫描走完整重建(与旧行为一致),之后进入增量。

### 性能

1. **聚合收尾改为增量**(`lib/session-source.mjs` 的收尾分支 + `lib/core.mjs` 的
   `unfoldSessionInto` / `incrementalRebuildInto`)。
   旧行为:`dirty` 为真就 `rebuildAllChunked(全库)` —— 代价 O(全库记录数),与"改了多少"
   完全脱钩,20 万请求的库改 1 个会话也要重折 20 万条(作者实测 447 ms)。
   新行为分三条路:

   | 情形 | 走哪条 |
   |---|---|
   | 数据变了(会话记录被替换 / 删除 / 修剪) | **增量**:只重折被改动的会话 |
   | 改价格 / 高峰时段 / 保留期 | **整库重折**(价格一改,每条记录的 cost 都过期) |
   | `force` 全量扫描 / 冷启动 / 聚合不可用 | **整库重折**(没有"旧账"可减) |

   端到端实测(真实 `scanStore` 路径,6 万请求 / 200 会话 / Node 24):

   | 场景 | 0.9.4 | 0.9.5 |
   |---|---|---|
   | 无变化扫描 | 0.2 ms | 0.2 ms |
   | **改 2 个会话** | **66.6 ms**(整库重折) | **2.7 ms** |
   | 冷启动首扫 | 整库重折 | 整库重折(同路径) |

   即"有变更的那一轮"约 **25×**,且库越大差距越大(O(改动量) vs O(全库))。
   两条路都会在 `summary.aggMode` 里如实报出 `full` / `incremental` / `none`,
   所以"这一轮到底重折了什么"是可观测的,不必靠计时猜。

   > **足量说明(别把这条读成"扫描不再花钱了")。** 增量只省掉"重折"那一段;读取与
   > 折叠会话日志本身的开销一点没减 —— 变更的会话仍要被完整读出来并 `foldSession`。
   > 冷启动、`force`、以及任何改价的时刻仍是整库重折。本版**没有**做"日志的增量解码"。

2. **等价性是硬门禁**(`test/v095-incremental.test.mjs` 的 V67 / V67e)。
   增量与全量在 25 轮随机改动(追加 / 缩短 / 删除 / 新增四种混跑)后,`days` / `months` /
   逐记录派生字段与 `sessions` 必须逐值相同;查询与导出的 CSV 必须**逐字节**相同。
   一个补充口径:聚合桶里的**汇总额**允许 1e-9 元的浮点公差(浮点减法不可逆,实测末位
   差 ~1e-17),而四段用量与请求数是**精确**判定 —— 这个区别本身由 V69 锁定。

### 修复

3. **`newRequests` 统计虚高**(`lib/session-source.mjs`)。此前是
   `+= 本会话全量条数`,于是每次会话增长都把它整段再加一遍 —— 一个 3000 条的会话被改
   10 次就虚报 3 万。现在是**净新增**条数。自测统计失真比没有统计更糟:它看起来像个
   真实指标。

4. **隔离清单在设置页从未渲染**(`web/src/app/80-tab-settings.js`)。服务端一直把
   `quarantinedCount` / `quarantined` 放在 `meta` 里(README 也说"设置页可见"),
   但页面里**一处都没用到** —— "有些会话统计不到"这件事对用户完全不可见。
   现在新增「数据完整性」一节:总数、每个被跳过会话的 id / 错误类型 / 命中次数 / 错误
   消息,并写明"保留上一次成功扫描的统计,**不会静默清零**"。超过服务端 50 条上报上限时
   显示"仅列出前 50 个 · 共 N 个"。错误消息来自宿主解码器,按外部数据处理并转义。

### 文档

5. **README 修正"改外观"指引**。原文让用户改 `web/src/**` 后跑
   `node scripts/build-web.mjs`,但 `package.json` 的 `files` 只发 `web/index.html` ——
   `web/src/**` 与 `scripts/**` **不在包里**(由 V49/V50 锁定)。npm / git / tarball
   三种安装方式下照做会扑空。已写明"这条只适用于 clone 仓库"。
6. **README 规模表补三行**:有变更的扫描周期已改走增量(附上表实测数据)、
   `store.json` 体积随会话数线性增长(它每个脏周期整体重写,本机 64 会话约 32 KB)、
   以及 `aggMode` 可观测。

### 与旧版的行为差异(需要知道的三条)

- **`cost` / `saved` 的浮点末位可能与 0.9.4 不同**(聚合桶汇总额吸附到 1e-12 元网格)。
  这是为了消除增量减法的累积噪声,量级远小于任何有意义的金额;逐记录 `r.cost` 不受影响。
- **`days` / `months` 的键顺序变为规范序**(字典序,含 `models` / `hours` 二级字典)。
  数值不变;`seriesQuery` 的输出字段顺序因此稳定下来。
- **`aggregatesNeedRebuild` 现在会拦住"有 requests 但 days 为空"这个矛盾态**,
  `aggregatesReady` 对它返回 false。此前它被认为是自洽的(真空库确实是),而改成按需重折
  之后这个中间态会一直留着,导致每个查询都拿到一张空账。

### 开发期工具(不进发布包)

- `scripts/bench-incremental.mjs` —— 增量 vs 全量的等价性与性能对照(固定种子,可复现)。
- `scripts/bench-scan-e2e.mjs` —— 走完整 `scanStore` 路径的端到端对照。

## 0.9.4

**主题:把"轻量高性能"里三处会随规模暴露的短板补掉 —— 冷启动不再独占事件循环、
分页不再是沉默的截断、转义从纪律变成结构。** 无存储格式变更(`STORE_VERSION` 仍为
v8),**升级不需要迁移动作**。

### 新功能

1. **冷启动的磁盘 IO 不再串行阻塞宿主**(`lib/index.js` 的 `loadStore` → 异步)。
   此前分片载入是 `readdirSync` + 逐分片 `readFileSync` + `JSON.parse`。
   现改走 `core.loadShardsAsync`(fs.promises + **并发 8**)。并发有上限:无上限会在
   数千分片时同时打开数千个文件描述符(Windows 上尤其容易撞句柄压力)。
   坏分片逐个报文件名(读取失败与 JSON 损坏都会回调),不再只说"少了几个"。
   记录与同步 `joinStore` **逐字段相同**(由 V54 锁定)。

   > **足量说明(别把这条读成"冷启动完全不阻塞了")。**
   > 用"预排一个探针、看它被延迟多久"的口径在 400 会话 / 20 万请求库上实测:
   >   同步载入(读+解析+展开)**276 ms** → 异步载入 **56 ms**。
   > 剩下的 56 ms 是**解析与对象展开**:分解测得 `JSON.parse` 400 个分片约 **31 ms**、
   > `joinStore` 展开成 20 万个记录对象约 **28 ms** —— 这两段是纯 CPU,同步执行,
   > 本版**没有**把它切成让出的步骤(那需要把展开也做成分片异步)。
   > 也就是说:0.9.4 消掉的是**磁盘 IO 的串行等待**(占比最大的那段),
   > 冷启动仍有一段几十毫秒的同步 CPU,规模越大越明显。这条不做过度承诺。

2. **扫描收尾的重建不再独占事件循环**(`core.rebuildAllChunked`)。
   重建在 20 万请求库上约 **447 ms**,而它发生在每 5 分钟一次的周期扫描收尾。
   逐会话累计、每片让出事件循环(默认 8 ms):
   同一探针口径实测**最坏单次让出间隔 177 ms → 28 ms**,而
   `days`/`months`/`sessions` 与逐记录字节**完全相同**(由 V57 锁定)。
   算法只有一处定义:同步的 `rebuildAll` 与分片的 `rebuildAllChunked`
   共用同一个 `rebuildSessionInto`。
   > `rebuildAll` 仍用于查询前的同步补齐(调用方本来就是同步的),语义未变。
   > 分片大小只是**调度粒度**,不改变任何计算结果。

3. **会话列表分页**(`/dsh-token/api/sessions?meta=1`)。
   `limit` 其实早已被钳到 `[1,500]`,但**没有任何分页契约**:第 501 个会话不是
   "要翻页才能拿到",而是**根本拿不到**,且响应里没有任何字段说明被截断 ——
   调用方无法分辨"一共 400 个"与"一共 5 万个我只看到 500 个"。
   现在 `meta=1` 时返回 `{ sessions, total, limit, offset, hasMore }`,
   `offset` 可翻页(上限 5000)。
   **默认(不带 `meta=1`)返回体仍是原来的数组**,键与形状与 0.9.3 完全一致。

4. **结构化转义**(`web/src/app/10-utils.js` 的 `tpl` / `raw`)。
   本页近 40 处 `innerHTML` 里混着用户可控字符串(会话标题、工作目录、模型名、
   搜索词 —— 都源自本机会话日志)。此前**全靠"每次都记得 `esc()`"**:
   逐条核对过、现有代码没有漏,但新增一处 `+ m.title +` 就能悄悄破掉它,且不报错。
   现在提供 `tpl\`…${x}…\``(插值默认转义)与 `raw()`(显式豁免),
   并把最高风险的几处(会话行、空状态、筛选胶囊、图表提示、日历格)
   改成结构化写法。`emptyHTML` 也改为接受 `raw()`。
   > 名字取 `tpl` 而不是 `html`:本页有近十处局部变量就叫 `html`,叫 `html` 会被
   > `var html = …` 静默遮蔽(表现为某个函数里 "html is not a function")。
   > 由 **V64** 做源码级守卫:新增的裸拼接会被测试拦住。

5. **不变量登记表**(`scripts/invariants.mjs` + `test/invariants.test.mjs`)。
   本插件有 95 条防退化守卫,每一条都对应一次真实事故,但它们的**理由**此前只散在
   代码注释与 92 KB 的 CHANGELOG 里 —— 新贡献者要动一行聚合代码得先读完 CHANGELOG
   才敢判断"这个判断能不能删"。现在登记表把"有哪些不变量、各自锁什么、为什么需要、
   被哪个测试锁住"变成可查询的数据,并由测试**双向强制**:
   登记了却没测试 → 失败(说明守卫被删了);有 V 编号测试却没登记 → 失败
   (说明新增守卫没写理由)。缺号必须在 `GAPS` 里解释。

### 修复与加固

6. **清掉 `store.json` 里残留的 `webPagePath`**。
   该配置 0.8.9 已移除(写入侧 400 拒绝、读取侧忽略),但**老库里的值从未被删除** ——
   而它的内容是一个**本机绝对路径**(形如
   `C:/Users/<name>/.dsh/profiles/web/node_modules/@fufuf-c/dsh-token/web/index.html`)。
   它会随 store.json 一起进入备份、issue 附件、截图,泄漏目录结构与用户名,
   而该字段早已不承载任何功能。
   现在**每次配置写入**(改价 / 改时段 / 改保留期)顺手清掉它。
   只删这一个已知键,不做"白名单之外都删" —— 那会让降级回本版时抹掉未来版本的配置。

7. **空库兜底不再可能变成清库路径**。`loadStore` 改异步后,存在一个真实窗口:
   路由已注册而分片还在读,此时到达的查询会拿到 `store === null`
   (此前 `loadStore` 同步,apply 返回时必然已就位,所以没有守卫)。
   兜底必须是**一次性只读对象**:若把它赋回 `store`,周期扫描会看到"store 非空"
   而不去 await 载入,在空库上跑完一轮并落盘 —— 把用户历史覆盖成空库。
   (这条自我纠正写进了代码注释,并由 V62/V62b 一族的行为断言围住。)

### 交付治理

8. **`package.json` 的 `files` 逐个列出运行时文件**,不再整目录收 `lib`。
   由此把 `lib/design-tokens.mjs`(8.3 KB,**仅构建期注入器使用**,运行时无任何
   import)移出发布包。新增 **V63b** 防止它被运行时 import —— 那会导致用户侧
   `MODULE_NOT_FOUND`,而仓库里(有那个文件)永远跑得通。

9. **去除不必要的叙述与遗留物**(按"能验证才删"的标准,不是凭感觉)。

   注释:交付代码里的**叙述性注释全部去除**,依据是实验 —— `lib/` 去注释后
   `node --check` 通过、385 项测试全绿,即注释不承载任何行为。
   `lib/` 214 → 162 KB,`web/src/` 185 → 158 KB,页面 **226 → 173 KB**。
   保留的是**机器锚点**而非说明:`dtk:tokens:begin/end`(注入锚点,由 V47b 断言)。

   遗留物(逐条核实后删除):
   - `scripts/split-web.mjs` + `_fix-parts.mjs` + `assertPartsCover`:`split-web` 是
     "从单文件切出源文件"的**一次性迁移工具**,早已跑完;`PARTS` 的行区间只有它和
     自检在读,而 `build-web` **只用文件名** → 区间是纯粹的过期元数据。
     现在 `PARTS` 就是一张文件名清单(20 项),少了"改源码就要重算行号"这件事。
   - 5 个无人引用的脚本(`dev-server` / `e2e-migration` / `e2e-real-scan` /
     `params-equivalence` / `probe-known-issues`):无 npm script、无测试、无 README、
     无其它脚本引用。
   - `docs/DESIGN-NOTES.md`(90 KB)与 4 份 `ANALYSIS-*` / `HANDOVER-*`(共 57 KB):
     `HANDOVER-0.9.1.md` 自称"当前装在 profile 的是 0.9.3"(实际已 0.9.4);
     `ANALYSIS-*` 分析的是 0.8.x 工作树且结论早已落地。
     这些"为什么"现在由 `scripts/invariants.mjs` 承载,且是**机器校验**的。

   core.mjs 里 `OFFICIAL_PROVIDER` / `priceCacheFor` 去掉 `export`(仅内部使用)。

10. **包体:232 KB → 126 KB**。页面 172.7 KB。README 42.6 → 37.9 KB,
    CHANGELOG 92 → 11.5 KB。

### 测试

断言从 344 项增至 **385 项**(+41)。新增三个测试文件:
- `test/escape-structural.test.mjs`(14 项)—— `tpl`/`raw` 语义 + 真实渲染函数在
  恶意输入下的行为 + 源码级裸拼接守卫;
- `test/v094-features.test.mjs`(18 项)—— 异步载入等价性/容错/并发上限、
  分片重建与同步重建的逐字段等价、分页契约、`webPagePath` 清理;
- `test/invariants.test.mjs`(11 项)—— 登记表的双向强制与编号纪律。

验证过的关键不变式:
- `rebuildAllChunked` 与 `rebuildAll` 的 `days`/`months`/`sessions`/逐记录**字节相同**;
- `loadShardsAsync` 与同步 `joinStore` 的记录**逐字段相同**;
- 20 万请求压力库:重建最坏单次让出间隔 **177 ms → 28 ms**。

## 0.9.3

**主题:刷新节奏交给用户配;并且"打开界面就看最新"。** 无存储格式变更
(`STORE_VERSION` 仍为 v8),但 `store.config` 新增一个 `refresh` 段;老库没有这个字段,
读出来就是默认值,**升级不需要迁移动作**。

## 0.9.2

**主题:把「刷新」按钮从"碰巧对"改成"结构上对"。** 只动网页层,无存储格式变更
(`STORE_VERSION` 仍为 v8),升级不需要任何迁移动作。

## 0.9.1

**主题:修掉 0.9.0 分片布局里的一个静默数据错位,并继续压内存与落盘开销。**

本版把 `STORE_VERSION` 由 **v7 升到 v8**(分片的模型表口径变了)。因为这次只动
"落盘字节怎么写、怎么读",没动 `foldSession` 语义与聚合口径,所以**升级不触发全量重扫**
(见下)。

## 0.9.0

**主题:轻量 + 高性能 + 高效,并收口遗留的正确性问题。** 本版是 **1.0 的预演**,
**允许破坏性变更**:`STORE_VERSION` 由 v6 升到 **v7**(落盘格式改变),落盘布局
由单文件改为 **`store.json` + `shards/`**。功能与页面不变,改动集中在数据层与构建层。

全部结论都有配对中位数实测支撑。测量环境:Node 24 / Windows;数据源为本机真实库
(45 会话 / 5019 请求)与合成压力库(`scripts/gen-stress-store.mjs`,400 会话 /
200000 请求 / 120 模型)。

## 0.8.10

**主题:治病根。** 消除"聚合运行时状态不可见"这一结构缺陷,并**由它查出 0.8.9 遗漏的两个真缺陷**。
本版**不改行为、不改 store 形状、不动 `STORE_VERSION`**,是纯内部重构 + 两个缺陷修复。

## 0.8.9

**主题:性能兑现 + 三个静默缺陷。** 本版**改行为**,且含**一处破坏性变更**(换肤功能移除)。

上一版(0.8.7)发布后做了一轮独立锐评,实测出三件事:README 的三个性能承诺全部注水、
成本字段的惰性访问器带来 9 倍内存放大、以及一个能让聚合**永久冻结**的缺陷。
本版逐条修掉,并**把 README 里夸大的数字换成实测值**。

## 0.8.7

**主题：修掉"一操作就退出"的自定义日期，重做活跃度热力图，并把侧边栏入口交还给外壳。** 三条都改行为。

## 0.8.5

**主题：把"数字可信"补上，并修掉一批界面失效。** 与 0.8.0 相反，这一版**改行为**。

先说最要紧的一条：**定价规则收紧了**。0.8.5 起只有 DSH 内置的官方模型
（`deepseek-official`）套用内置价目，**其余一切 provider 一律不计价**（成本记 0，
由"未计价"显式报出）。此前第三方路由会按模型名套官方价估算，但中转的真实计费与
官方价无关，那个数字**像账单却不是账单**，比老实说"没价"更容易误导。需要精确成本
请在单价表为这些模型录入自定义单价 —— 那条路径不受影响。

## 0.8.0

**主题：纯重构。使用者没有任何可感知的变化。**

坦白说：这一版**不加功能、不改界面、不动 `store.json` 结构**，界面外观与所有数字逐字节
不变（有快照与真实数据对比为证，见"说明"）。它对**使用者是零影响**；收益只落在维护者
一侧 —— 同一段口径原先散落在 30 处，以后改口径只需改 1 处。

之所以值得单独发一版，是因为这种重复的危险在"改口径"那一刻才现形：四处改了三处、
漏掉的那一处**不会报错**，只会安静地显示一个和总额对不上的数字。

> - **`foldSession` 语义与存储格式未触碰**：`STORE_VERSION` 仍为 **v6**。

## 0.7.1

**主题：把"抄一份值"改成"接宿主变量"。** 0.7.0 及以前，面板的字体、字号、底色都是从宿主
样式里复制出来的固定值 —— 复制值今天看着对，但用户一改设置、或宿主一换主题就静默失效，
而界面仍然"看起来正常"，没人会发现。

> - 无破坏性改动：三个入口（侧边栏 / 对话区页签 / 设置页）的名称、插槽与 API 契约不变，
